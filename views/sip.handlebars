<section class="container review-list my-5">
  <table class="table">
    <thead>
      <tr style="color:white;">
        <td scope="col">Tutor Name</td>
        <td scope="col">Rating</td>
        <td scope="col">Review</td>
        <td scope="col">Availability</td>
      </tr>
    </thead>

    {{#each reviews as |review|}}
      <tr style="color:white;">
        <td>
          {{review.tutor.name}}
        </td>
        <td>
          {{review.rating}}
        </td>
        <td>
          {{review.review}}
        </td>
        <td>
          {{#if review.daytime}}
            Daytime
          {{/if}}
          {{#if review.weekends}}
            Weekends
          {{/if}}
          {{#if review.nights}}
            Nights
          {{/if}}
        </td>
      </tr>
    {{/each}}
  </table>
</section>